name: Cleanup merged documentation branch

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write

jobs:
  cleanup:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'docs/project-description-and-migration'
    runs-on: ubuntu-latest
    steps:
      - name: Delete merged documentation branch
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: docs/project-description-and-migration
        run: |
          set -euo pipefail
          gh api \
            --method DELETE \
            "repos/${GITHUB_REPOSITORY}/git/refs/heads/${BRANCH}"
          echo "Deleted ${BRANCH}."
